id=${moduleId}
name=${moduleName}
version=${versionName}
versionCode=${versionCode}
author=silvzr
description=Prevents root detection via mount info leakage by hooking setmntent/endmntent. Requires ReZygisk.
#updateJson=
